spring:
  application:
    name: eureka-server

server:
  port: 9500

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    service-url:
      defaultZone: http://localhost:9500/eureka